import { useQuery, useMutation,useQueryClient } from "@tanstack/react-query";
import { projectList,deleteProject } from "./api";
import type { Project } from "./types";
export default function ProjectItem(){
    
    return(<></>)
}